# Jarvis v2 — Voice Bot

Auto-pushed from Helsinki after each deploy. Source of truth for the live service.
No secrets in repo — all API keys live in /opt/jarvis-v2/.env (ignored).

Raw URLs for read-only inspection:
  https://raw.githubusercontent.com/sechshundertneunzaehn-cpu/jarvis-v2/main/<path>
